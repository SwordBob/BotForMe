# 工作空间文件清单报告

## 📊 概览
- **检查时间**: $(date '+%Y-%m-%d %H:%M:%S')
- **工作空间**: /Users/niejq/.openclaw/workspace
- **总文件数**: $(find . -type f ! -path "./.git/*" | wc -l)
- **Git 仓库**: $(git rev-parse --is-inside-work-tree 2>/dev/null && echo "已初始化" || echo "未初始化")

## 📁 目录结构

### 根目录文件
$(ls -la | awk 'NR>1 {print "  - " $9 " (" $5 "字节, " $6 " " $7 " " $8 ")"}' | grep -v "^  - \.$" | grep -v "^  - \.\.$")

## 📝 文件分类统计

### Markdown 文档 (.md)
$(find . -name "*.md" -type f ! -path "./.git/*" | wc -l) 个文件

**文件列表:**
$(find . -name "*.md" -type f ! -path "./.git/*" | while read file; do
    size=$(wc -l < "$file" 2>/dev/null || echo "0")
    git_status=$(git status --porcelain "$file" 2>/dev/null)
    if [ -z "$git_status" ]; then
        status="✅ 已提交"
    elif echo "$git_status" | grep -q "^??"; then
        status="❌ 未跟踪"
    elif echo "$git_status" | grep -q "^ M"; then
        status="⚠️  已修改未提交"
    elif echo "$git_status" | grep -q "^M "; then
        status="📝 已暂存未提交"
    else
        status="❓ 未知"
    fi
    echo "  - $file ($size 行) - $status"
done)

### Shell 脚本 (.sh)
$(find . -name "*.sh" -type f ! -path "./.git/*" | wc -l) 个文件

**文件列表:**
$(find . -name "*.sh" -type f ! -path "./.git/*" | while read file; do
    echo "  - $file"
done)

### Python 脚本 (.py)
$(find . -name "*.py" -type f ! -path "./.git/*" | wc -l) 个文件

**文件列表:**
$(find . -name "*.py" -type f ! -path "./.git/*" | while read file; do
    echo "  - $file"
done)

### 配置文件 (.json, .plist, .yml, .yaml)
$(find . \( -name "*.json" -o -name "*.plist" -o -name "*.yml" -o -name "*.yaml" \) -type f ! -path "./.git/*" | wc -l) 个文件

## 🔍 搜索特定文件

### 搜索 "Obsidian" 相关文件
$(find . -type f -iname "*obsidian*" ! -path "./.git/*" 2>/dev/null | while read file; do
    echo "  - $file"
done || echo "  未找到相关文件")

### 搜索 "关系图谱" 相关文件
$(find . -type f -name "*.md" -exec grep -l "关系\|图谱" {} \; 2>/dev/null | while read file; do
    echo "  - $file"
done || echo "  未找到相关文件")

## 📊 Git 状态摘要

### 分支信息
- **当前分支**: $(git branch --show-current 2>/dev/null || echo "未知")
- **最新提交**: $(git log -1 --pretty=format:"%h - %s" 2>/dev/null || echo "无提交")

### 文件状态
- **已跟踪文件**: $(git ls-files | wc -l)
- **未跟踪文件**: $(git status --porcelain | grep "^??" | wc -l)
- **已修改未提交**: $(git status --porcelain | grep "^ M" | wc -l)
- **已暂存未提交**: $(git status --porcelain | grep "^M " | wc -l)

### 未跟踪文件列表
$(git status --porcelain | grep "^??" | cut -c4- | while read file; do
    echo "  - $file"
done || echo "  无未跟踪文件")

### 已修改未提交文件
$(git status --porcelain | grep "^ M" | cut -c4- | while read file; do
    echo "  - $file"
done || echo "  无已修改未提交文件")

## 🎯 缺失文件检查

### 检查 "Obsidian-关系图谱分析报告.md"
- **状态**: $(if [ -f "Obsidian-关系图谱分析报告.md" ]; then echo "✅ 存在"; else echo "❌ 不存在"; fi)
- **Git状态**: $(if git ls-files "Obsidian-关系图谱分析报告.md" &>/dev/null; then echo "✅ 已跟踪"; elif [ -f "Obsidian-关系图谱分析报告.md" ]; then echo "❌ 未跟踪"; else echo "⚠️  文件不存在"; fi)

### 检查 "《Obsidian 关系图谱分析》.md"
- **状态**: $(if [ -f "《Obsidian 关系图谱分析》.md" ]; then echo "✅ 存在"; else echo "❌ 不存在"; fi)

## 💡 建议

### 立即操作
1. **添加未跟踪文件到 Git**
   ```bash
   git add .
   git commit -m "添加未跟踪文件"
   ```

2. **提交已修改文件**
   ```bash
   git add -u
   git commit -m "提交修改"
   ```

3. **创建缺失文件**（如果需要）
   ```bash
   # 创建 Obsidian-关系图谱分析报告.md
   echo "# Obsidian 关系图谱分析报告" > "Obsidian-关系图谱分析报告.md"
   git add "Obsidian-关系图谱分析报告.md"
   git commit -m "添加 Obsidian 关系图谱分析报告"
   ```

### 长期维护
1. **定期检查文件状态**
2. **及时提交更改**
3. **保持文档更新**
4. **备份重要文件**

---
**报告生成时间**: $(date '+%Y-%m-%d %H:%M:%S')
**生成者**: OpenClaw AI 助手