
## 待办事项

### OpenClaw Gateway 进程保护配置
- **问题**：Gateway 通过手动启动（非 LaunchAgent），系统重启后会中断
- **状态**：待处理
- **风险**：系统重启后需要重新手动启动 Gateway
- **下一步**：
  1. 停止当前的 Gateway 进程（PID 54842）
  2. 启用 LaunchAgent（ai.openclaw.gateway.plist）接管
  3. 验证 LaunchAgent 正确加载并保持 Gateway 运行
- **备注**：LaunchAgent 的 plist 已存在且配置正确，只需重启即可生效

